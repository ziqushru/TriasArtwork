<%= form_for(christening) do |f| %>
  <% if christening.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(christening.errors.count, "error") %> prohibited this christening from being saved:</h2>

      <ul>
      <% christening.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :video_id %>
    <%= f.text_field :video_id %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
