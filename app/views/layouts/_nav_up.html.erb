<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="dropdown">
                <button class="button_custom_animated dropdown-toggle" type="button" data-toggle="dropdown">
                    <span>Photolens</span>
                </button>
                <div class="dropdown_content">
                    <%= link_to "Home", root_path(:controller => "info", :action => "home") %>
                    <%= link_to "Christenings", gallery_showAll_path(:controller => "gallery", :action => "showAll", :option => "christening") %>
                    <%= link_to "Weddings", gallery_showAll_path(:controller => "gallery", :action => "showAll", :option => "wedding") %>
                </div>
            </div>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav" style="float: right">
                <% if @option %>
                <li>
                    <button class="button_custom">
                        <a href="<%= gallery_showAll_path(:option => @option) %>">Gallery</a>
                    </button>
                </li>
                <li>
                    <button class="button_custom">
                        <a href="<%= services_show_path(:option => @option) %>">Services</a>
                    </button>
                </li>
                <% end %>
                <li>
                    <button class="button_custom">
                        <a href="<%= info_bio_path(:option => @option) %>">Bio</a>
                    </button>
                </li>
                <li>
                    <button class="button_custom">
                        <a href="<%= info_contact_us_path(:option => @option) %>">Contact Us</a>
                    </button>
                </li>
                <% if current_user %>
                <li>
                    <button class="button_custom">
                        <a href="<%= new_image_path(:option => @option) %>">New Image</a>
                    </button>
                </li>
                <li>
                    <button class="button_custom">
                        <a href="<%= images_destroy_path(:option => @option) %>">Destroy Image</a>
                    </button>
                </li>
                <li>
                    <button class="button_custom">
                        <a href="<%= images_showAll_path(:option => @option) %>">Show All Images</a>
                    </button>
                </li>
                <li>
                    <button class="button_custom">
                        <a href="<%= destroy_user_session_path %>">Log Out</a>
                    </button>
                </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row">
        <div class="col-lg-12 text-center">
            <% flash.each do |a, b| %>
                <div class="alert alert-success"> <%= b %> </div>
            <% end %>